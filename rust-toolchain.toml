[toolchain]
channel = "nightly"
components = [
	"rustfmt",
	"clippy",
	#"rustc-dev",
	#"rust-src",
	#"llvm-tools-preview",
	#"rust-std",
]
#profile = "minimal"
#panic = "abort"
#codegen-units = 1
#lto = true
#opt-level = "z"
#strip = true

# [profile.release]
# strip = true # src/bin, Strip symbols from binary*
# # https://doc.rust-lang.org/cargo/reference/profiles.html#release
# opt-level = 1      # 'z'
# incremental = true
# #lto = true          # Enable Link Time Optimization
# #codegen-units = 1   # Reduce number of codegen units to increase optimizations.
# #panic = 'abort'     # Abort on panic
# [profile.release.package."*"]
# opt-level = 3
# [profile.dev]
# opt-level = 1
# [profile.dev.package."*"]
# opt-level = 3
# # The profile that 'cargo dist' will build with
# [profile.dist]
# inherits = "release"
# lto = "thin"

